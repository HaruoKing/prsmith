# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Swift executable package initialized with Swift Package Manager
- `swift-argument-parser` dependency and root `PRSmith` command with `--help` output
- Module folder structure (`Commands/`, `Git/`, `Markdown/`, `Models/`, `Services/`, `Utilities/`)
- README with installation, usage, examples, and roadmap
- MIT License
- CONTRIBUTING guide
- CI workflow that builds and tests on every pull request
- Branch protection on `main` requiring review and passing CI
